<%= form_with(model:[book, book_comment], local: false) do |f| %>
  <%= f.text_area :comment, rows:'5',placeholder: "コメントをここに", class: "w-100", class:"comment_textarea" %>
  <%= f.submit "送信" %>
<% end %>